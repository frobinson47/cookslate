node_modules/
frontend/dist/
uploads/recipes/*
!uploads/recipes/.gitkeep
api/.env
api/vendor/
api/logs/
.env
docs/plans/
cookies.txt
api/.phpunit.result.cache
.claude/
CLAUDE.md
gitleaks-*.json
docs/screenshots/
api/config/license.key
api/config/license.private.pem
docs/
*.sql
crumble_db_backup.sql

# Autonomous Forge session files
.forge/sessions/

# Local tool caches/state
.graymatter/
.wrangler/

# Local-only marketing assets (not shipped in the repo)
marketing/

# Local knowledge scaffold (global CLAUDE.md instructions, not project-specific)
knowledge/

# Infisical machine-identity creds for prod deploy (server-side only, never committed)
.infisical-auth
