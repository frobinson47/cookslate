# Autonomous State

- Current roadmap version: v3
- Current task ID: none
- Current task status: Roadmap v3 is now fully complete. AUTO-026 through AUTO-030 all DONE and deployed. No known TODO items remain anywhere in the plan file. Since AUTO-030 shipped, README.md was refreshed to list all features added since the original launch (commit 715b3e7) and the separate cookslate-server marketing site (cookslate.app landing page) was updated to match — that repo is outside this one, no plan-file entry for it.
- Current branch: master
- Last run timestamp: 2026-07-31T16:03:16-04:00
- Last successful commit hash: 715b3e7
- Latest run summary: Shipped AUTO-030 (household permissions), the last item in Roadmap v3, after a dedicated scoping discussion with the user (logged at decisions/2026-07-30-household-permissions-scope.md). Key finding that shrank the scope: Cookslate has no multi-tenant concept — a self-hosted instance already IS the household, so no new `households` table was needed. Delivered: (1) migration 027 adds a `viewer` role to `users.role`; `Auth::requireWriteAccess()` blocks viewer sessions and is now applied to essentially every mutating endpoint app-wide, not just the newly-shared resources. (2) Collections, grocery lists, and pantry are now visible to every user on the instance (dropped the `WHERE created_by/user_id = ?` filter on list/read queries), tagged with `is_owner`/`created_by_username` so the frontend can gate controls; edit/delete stays creator-or-admin-only via the existing ownership-check pattern, now with an admin-override option added to Pantry and MealPlan's item mutations to match Collection/GroceryList. Pantry's "already stocked" checks (`isInPantry`/`getPantryMatches`) also went household-wide. (3) Meal plans deliberately kept their per-user-row model rather than merging into one shared grid (to avoid breaking the existing drag-and-drop day/sort_order logic) — added a `GET /users/household-members` endpoint and a "My plan / {member}'s plan" switcher on MealPlanPage to browse another member's week read-only. (4) Recipes, favorites, ratings, cook log, shopping trips, and API keys were explicitly left unchanged, as scoped. Frontend: Collections/Grocery/Pantry pages gray out or hide edit/delete/add affordances for items the caller doesn't own (unless admin), and AdminPage's role picker gained a Viewer option. 7 new PHPUnit tests, full suite 316/316 passing, frontend build/lint clean. Deployed to prod (hookhouse-pro): migration applied by copying the SQL into the running app container and executing via its PDO connection (same technique as AUTO-027, since the container doesn't mount `database/`), then `deploy.sh`. Verified live: role enum confirmed updated on the prod DB, all touched routes return correct auth-gated status codes on the actual prod domain `home.cookslate.app`. Forgejo issue #82 closed with an explanatory comment.
- Files changed in the latest run: database/migrations/027_viewer_role.sql (new); api/middleware/Auth.php (requireWriteAccess); api/models/Collection.php, GroceryList.php, Pantry.php, User.php, pro/models/MealPlan.php; api/controllers/CollectionController.php, GroceryController.php, PantryController.php, RecipeController.php, FavoriteController.php, RatingController.php, CookLogController.php, RecipeShareController.php, TagController.php, UserApiKeyController.php, UserController.php, ShoppingTripController.php, pro/controllers/MealPlanController.php; api/index.php; api/tests/Unit/HouseholdSharedVisibilityTest.php (new); frontend/src/hooks/useAuth.jsx (isViewer), services/api.js (getHouseholdMembers, user_id param), pages/AdminPage.jsx, pages/CollectionsPage.jsx, pages/GroceryPage.jsx, pro/pages/MealPlanPage.jsx, components/grocery/GroceryItem.jsx, GroceryList.jsx, PantrySection.jsx; decisions/2026-07-30-household-permissions-scope.md (new); .ai/AUTONOMOUS_PLAN.md.
- Validation commands and results: `php -l` clean on all touched PHP files. `vendor/bin/phpunit tests/Unit/HouseholdSharedVisibilityTest.php` — 7/7 pass. `vendor/bin/phpunit` (full suite) — 316/316 pass (12 pre-existing skips). `npm run lint` and `npm run build` (frontend) — both clean.
- Current blockers: None.
- Known risks and assumptions: NOT manually clicked through in a live browser this pass — no test credentials were available locally without risking the 5-failed-attempt account lockout, so verification relied on backend tests, a build/lint pass, and live curl checks against the deployed API's auth gating. The viewer-role write-block and the meal-plan member-switcher in particular deserve a manual pass before considering this fully signed off. Existing Household-tier users will see a real behavior change on next login: other members' collections/meal-plans/grocery-lists/pantry items are now visible where they previously weren't — worth a release note if/when this ships publicly. Meal-plan grocery-list generation and template save/apply still only ever operate on the caller's own week (by design, not extended to "generate from the whole household's combined plan").
- Recommended next task: No TODO items remain in Roadmap v3. Natural next steps: (1) manually smoke-test the AUTO-030 viewer role and meal-plan switcher in a live browser session — this is the one meaningfully under-verified piece from this run; (2) more broadly, smoke-test the full AUTO-023 through AUTO-030 batch, since none of Roadmap v3 has been manually clicked through yet; (3) run another feature-enhance pass for a v4 roadmap once the user wants more scope.
